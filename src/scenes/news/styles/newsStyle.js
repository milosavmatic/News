import { StyleSheet } from 'react-native';

const NewsStyle = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
    },
    navTape: {
        width: '100%',
        height: '5%',
        flexDirection: 'row'
    },
})

export default NewsStyle;